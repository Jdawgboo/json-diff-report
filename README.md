# json-diff-report

Dependency-free, field-level comparison for two JSON objects.

```bash
python json_diff_report.py before.json after.json
python -m unittest -v
```

Lists additions, removals, and changed scalar values. MIT licensed.